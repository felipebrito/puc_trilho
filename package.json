{
  "name": "puc-trilho-master",
  "version": "1.0.0",
  "description": "Master starter for Kiosk and Hardware Bridge",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && cd trilho_app && npm install && cd ../automacoes/encoder_control && npm install",
    "start": "concurrently \"cd trilho_app && npm run dev\" \"cd automacoes/encoder_control && node bridge.js\"",
    "dev": "npm start"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}